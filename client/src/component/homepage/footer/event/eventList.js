export default function EventList() {
  // 1. display the events for given month
  // 2. given each a unique pastel color
  return (
    <div className="event_contain">
      <div className="event">Release</div>
      <div className="event">Fix css</div>
      <div className="event">Hackathon</div>
    </div>
  );
}
